## Cursor Cloud specific instructions

This repository is currently an empty/bare project containing only a `README.md`. There are no applications, services, dependencies, or build systems configured.

- **No package manager or dependency files** — no `package.json`, `requirements.txt`, `go.mod`, `Cargo.toml`, etc.
- **No build/test/lint tooling** — no Makefile, CI config, or test framework.
- **No services to start** — no Docker, docker-compose, or dev server configuration.

When code is added to this repository, update this section with relevant setup, build, test, and run instructions.
